// Simule une sortie de build (Vite/webpack) — un vrai plugin remplacerait
// ceci par son bundle React/Vue/Svelte compilé.
document.getElementById("root").textContent = "SPA Demo — servi tel quel par xcore, aucun import xui.";
